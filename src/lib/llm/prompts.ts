import { CATEGORIES } from "@/lib/types";
import {
  AI_SUMMARY_TARGET_MAX_CHARS,
  AI_SUMMARY_TARGET_MIN_CHARS,
  AI_TITLE_MAX_CHARS,
} from "@/lib/constants";
import type { CurationInput } from "./schemas";

const CATEGORIES_LIST = CATEGORIES.join("、");

/**
 * 想定読者の定義。openspec/specs/wedding-trend/spec.md §9.1 と**同一の文面**
 * （プロンプトと spec.md とで定義が乖離しないよう意図的に揃えている。変更する
 * 場合は両方を同時に更新すること）。
 *
 * この読者像を要約生成にも読ませることで、要約自体もこの読者が知りたい部分を
 * 残したものになる。これが、有用度判定（下記 USEFULNESS_CRITERIA_RULES）を
 * 採点専用の2パス目にせず、既存のキュレーション呼び出しと同一コールに乗せて
 * いる理由でもある。
 */
const READER_PERSONA = `# 想定読者
想定読者は、挙式日・会場が決まっており、衣装（ドレス・和装）もおおむね決まっている。これから決めるのは挙式・披露宴の中身である —— 進行とタイムライン、演出、席次と席札、余興、スピーチや余興の依頼、BGM、装花、料理、引出物、ペーパーアイテム、写真と映像、ゲストの過ごしやすさ、当日の段取り。この読者が求めているのは「何が人気か」ではなく「実際に式を挙げた人が、何を、なぜそう判断したか」である。`;

/**
 * 有用度判定 5 項目（すべて 0〜9）の指示。定義は spec.md §9.2〜§9.4 と同一の内容。
 *
 * 点数（重み付け）は絶対に出力させない。点数は `src/lib/scoring/usefulness.ts`
 * の `computeUsefulnessScore()` がコード側で計算する設計であり、LLM に点数を
 * 出させた瞬間に「重み調整を再課金ゼロのコード変更で完結させる」という設計
 * 全体が無効になる。
 *
 * 意図的に書いていないこと: 「衣装・ドレスの記事を減点せよ」という指示。
 * 衣装のみの記事は `ceremonyDecision` ゲート（コード側の
 * `computeUsefulnessScore`）で構造的に沈む設計のため、ここに二重で書く必要は
 * なく、「衣装のみではない」のような二重否定のブール項目を LLM に判定させると
 * 最も不安定になる。
 */
const USEFULNESS_CRITERIA_RULES = `# 有用度判定（5項目をそれぞれ 0〜9 の整数で判定。点数（合計）は絶対に出力しないこと）
上の想定読者にとって役に立つ投稿かどうかを、以下の各項目について 0〜9 の整数で採点する。

## スケールの使い方（最重要。ここを誤ると全記事が同じ点になり掲載順が機能しなくなる）
- **0 = 完全に該当しない**。
- **1〜3 = わずかに／間接的に該当**。触れてはいるが浅い。
- **4〜6 = はっきり該当（標準的な良記事）**。条件を満たす記事の多くはこの帯。
- **7〜8 = 同種記事の中で明確に上位**。複数の場面・具体例・理由が揃っている。
- **9 = ほぼ完璧。滅多に付けない**。この1項目だけで読者に強く推せるレベル。
- 記事どうしを相対比較して差をつけること。「良い記事だから満点」ではなく、他の当事者レポートと並べて一段違うか。同じ点を大量に付けない。迷ったら低めに。

※ ceremonyDecision と weddingDayContent は別軸：ceremonyDecision は「その話題が式の中身の意思決定に役立つか（トピック適合）」、weddingDayContent は「**厳密に挙式・披露宴の当日**の実施内容を扱っているか（対象範囲）」。

- firsthand: 書き手が実際に挙式・披露宴を経験した立場から書いているか。新婦本人に限らず、新郎・両家家族、およびプランナー・司会者・カメラマン・装花担当など式に立ち会う職能者の実務経験も含む。9に近い=当事者本人が自分の式について固有のエピソードを多数交えて書いている、中位=当事者だが記述が概説的または近しい立場からの一般論、0=第三者・まとめ・伝聞のみ。
- ceremonyDecision: 挙式・披露宴の中身の意思決定（進行・タイムライン・演出・席次・席札・余興・スピーチ・BGM・装花・料理・引出物・ペーパーアイテム・挙式当日の写真/映像・ゲストの過ごしやすさ・当日段取り）にどれだけ効くか。高い=複数の意思決定項目について「何を・なぜそう決めたか」の判断材料が豊富、中位=関わる話題に触れる、0=ほぼ無関係。
- specific: **当日の実施内容の具体性**。固有の選択・数値・実際にやったこと／やらなかった理由・現場でどう進んだかがどれだけ濃く書かれているか。高い=複数の場面で具体的な選択・数値・理由が詳述、中位=一部具体的だが要点は抽象的、0=心構え・一般論・感想のみ。
- weddingDayContent: **式当日に会場で実際に起きた事実**が、実体験として書かれているか。判定の第一関門は**時制**。
  - **過去形・完了形で「当日に起こったこと」が具体的に書かれている**=最高（例: 「入場曲が流れた」「料理が出た」「ゲストが泣いた」）。
  - **当日の予定・計画・手配・準備の延長**（「当日は○をする予定」「当日お渡しする」「先に出す」「〜てもらいます」等、予定形・未完了の手配）は**当日の実施内容ではない**。準備段階は weddingDayContent を上げない（specific には効いてよいが、ここでは対象外）。
  - 語の有無ではなく、**「当日に会場で起きた事実」 vs 「当日に向けた準備」**の区別で判定する。
  - 高い=当日の複数の場面が時系列で「起こったこと」として描写、中位=当日に起きた事実が断片的・1場面だけ、0=当日の事実描写が実質ゼロ（準備・予定・振り返りのみ）。
- promotional: 事業者による集客・自社サービスへの誘導の度合い。判別基準は「読者が別の会場・別の業者で式を挙げる場合にも役立つか」。0〜1=集客要素が実質的にない、中位=自社サービスへの言及や導線はあるが主目的は情報提供で他式場でも役立つ、高い（7以上）=文章中で過剰かつ明確に自社サービス・特定式場への誘導（減点対象。実例紹介に安易に高得点を付けない）。

「卒花」「花嫁レポ」「#プレ花嫁」等の語がタイトル・本文に含まれること自体は加点材料ではない。逆にこれらの語が一切無くても、実際の挙式・披露宴の経験に基づく知見であれば同等に扱うこと。

判断材料が本文抜粋に無い場合、各項目は 0 とせよ。本文にあるだろうと推測して点を付けてはならない。`;

const RATIONALE_RULES = `# topicAnchor のルール（必ず守ること）
- 目的: 読者が「まだ知らない何かが、そこに具体的にある」と分かるよう、記事の「視点・場面・争点」をアンカーで開示する。結論・結果・具体的数値は書かない（ゼロクリック化の回避）。
- クリック誘引: この記事ならではの独自性（他の記事には無い切り口・具体的な状況・意外な着眼点・当事者ならではの気づき）を一つ選び、それを読者が「続きを読みたい」と感じる形で提示する。ありふれた一般論ではなく、この1本を開く理由になる要素を出すこと。ただし煽り表現には頼らない（下記「禁止」参照）。文の形（問いかけ・体言止め・述語で結ぶ 等）は自由。
- 長さ: 18〜36字を目安に。10字台は短すぎる。上限40字。
- 語彙: 助詞・接続表現・述語・言い換えは自由。記事の内容に即していれば、本文抜粋にある語をそのまま使う必要はない（自然な日本語で言い換えてよい）。ただし記事が扱っていない話題を作り出さないこと。
- 禁止: 記事固有の具体数値（半角/全角/漢数字・金額・日付）、人物の氏名・ニックネーム・SNS アカウント名・個人と結びつく会場名・店舗名。煽り・価値付与（衝撃/必見/実は/知らないと損/最強/絶対/驚愕/やばい/話題/最高/究極/世界一 等）、二人称命令（〜しよう/〜すべき）、メタ表現（〜についての記事/〜を紹介 等）。
- 表題非重複: タイトルの単なる言い換えにしない。タイトルに無い視点・場面・争点を必ず一つ足すこと。`;

const FEWSHOT_ANCHOR = `# 例（タイトル → 弱いアンカー → 強いアンカー → なぜ強いか）

[例1]
タイトル: 結婚式準備を一人で進める心構え
弱い: 結婚式準備
強い: 結婚式をしたい人ではなかった
なぜ: 引用的視点で人間の声を出し、結論を完全に伏せ、タイトルと語彙が重ならない。

[例2]
タイトル: 式場見学で確認すべきポイント
弱い: 式場見学
強い: 館内案内で実際に何が起きているのか
なぜ: 具体的な場面を名指し、答えを伏せ、タイトルに無い視点を足す。

[例3]
タイトル: 見積もりを比較する際の注意点
弱い: 見積もり比較
強い: 見積もりを見るときに知っておいてほしいこと
なぜ: 読者の知りたい問いを立て、タイトル語に依存しない。

[例4]
タイトル: 結婚式直前に気づく準備の反省
弱い: 準備の反省
強い: もっと早くやればよかったこと
なぜ: 一人称の気づきを開示し、結論を漏らさない。

[負例A：タイトルのエコー]
タイトル: 祝儀袋の選び方とマナー
悪い: 祝儀袋の選び方
なぜ悪い: タイトルの言い換え。表題と被り、読者に新情報を与えない。

[負例B：煽り]
タイトル: 式場見学の件数と決定理由
悪い: 必見の理由
なぜ悪い: 「必見」は媒体の価値付与（煽り）。記事の視点ではない。

[注意：〜こと 名詞化は結論漏洩の型]
タイトル: 式場マネージャーが語る見積もりの仕組み
× 見積もりが上がること（結論を開示＝軽度のゼロクリック化）
○ 見積もりが上がる瞬間はどこか（結論を伏せる）`;

/**
 * すべてのプロンプトに共通する制約。著作権・事実性の観点で特に重要な指示：
 * - 原文の言い回し・文体をそのまま再現しない（要約は事実抽出であり創作的表現の複製ではない）
 * - 原文に無い事実（金額・会場名・日付等）を推測で補わない
 */
const SHARED_RULES = `# 制約（必ず守ること）
- タイトルは${AI_TITLE_MAX_CHARS}文字以内のニュース見出し調。誇張・煽り禁止。
- 要約は${AI_SUMMARY_TARGET_MIN_CHARS}〜${AI_SUMMARY_TARGET_MAX_CHARS}文字程度。投稿の要点・費用感・特徴を客観的に。
- 原文の独自の言い回し・文体をそのまま再現しないこと（著作権上の要件。要約は事実と要点の抽出であって、創作的表現の複製ではない）。
- 推測で事実（金額・会場名・日付など）を補完しない。原文に無い情報は書かない。
- カテゴリは以下の列挙から必ず1つ選ぶ: ${CATEGORIES_LIST}
- tag は、新しい/流行りの演出・アイテムなら "trend"、長年支持される王道・定番なら "classic"。
- firsthand / ceremonyDecision / specific / weddingDayContent / promotional は必ず 0〜9 の整数で出力すること（boolean・文字列・小数・9 超は不可）。
- 出力は JSON のみ。マークダウン・前置き・説明文は一切含めない。`;

function formatInput(input: CurationInput): string {
  return `タイトル: ${input.title}\n本文抜粋: ${input.excerpt ?? "（本文抜粋なし）"}`;
}

/** 単体キュレーション用プロンプト（バッチ失敗時のフォールバック、および SNS 単発投稿）。 */
export function buildSingleCurationPrompt(input: CurationInput, feedback?: string): string {
  const feedbackBlock = feedback
    ? `\n【再生成フィードバック】\n以下の理由で直前のアンカーが検証に失敗しました。同じタイトル・抜粋で、タイトルに無い視点を足したアンカーに直しなさい。\n<feedback>\n${feedback}\n</feedback>\n`
    : "";

  return `あなたはウエディング（結婚式）関連の SNS/ブログ投稿を、中立的な立場で要約するキュレーターです。
以下の投稿1件について、JSON のみを出力してください。

${READER_PERSONA}

${formatInput(input)}

${SHARED_RULES}

${USEFULNESS_CRITERIA_RULES}

${RATIONALE_RULES}

${FEWSHOT_ANCHOR}
${feedbackBlock}
出力形式:
{"title":"...","summary":"...","category":"...","tag":"trend","firsthand":5,"ceremonyDecision":5,"specific":5,"weddingDayContent":5,"promotional":0,"topicAnchor":"..."}
`;
}

/** バッチキュレーション用プロンプト。index は入力の 1 始まり番号と一致させる。 */
export function buildBatchCurationPrompt(inputs: CurationInput[]): string {
  const itemsBlock = inputs.map((input, i) => `${i + 1}.\n${formatInput(input)}`).join("\n\n");

  return `あなたはウエディング（結婚式）関連の SNS/ブログ投稿を、中立的な立場で要約するキュレーターです。
以下の ${inputs.length} 件の投稿それぞれについて要約してください。

${READER_PERSONA}

${itemsBlock}

${SHARED_RULES}
- index は入力の番号（1始まり）と必ず一致させること。
- 出力は JSON オブジェクトのみ。1件につき1オブジェクト、入力と同じ件数で漏れなく出力すること。

${USEFULNESS_CRITERIA_RULES}

${RATIONALE_RULES}

${FEWSHOT_ANCHOR}

出力形式:
{"items":[{"index":1,"title":"...","summary":"...","category":"...","tag":"trend","firsthand":5,"ceremonyDecision":5,"specific":5,"weddingDayContent":5,"promotional":0,"topicAnchor":"..."}, ...]}
`;
}
